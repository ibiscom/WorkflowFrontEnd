import { DocumentModelEntity } from "./document-model.entity";

/**
 * Entidad que representa una herramienta de evento de inicio en el sistema.
 */
export interface HerramientaEventoInicioEntity {
  code: string;
  name: string;
}